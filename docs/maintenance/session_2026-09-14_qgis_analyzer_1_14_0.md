# Session 2026-09-14 — qgis-plugin-analyzer 1.14.0 Upgrade

## 🎯 Objective
Adopt the newly published `qgis-plugin-analyzer` 1.14.0 and re-run the static analysis to refresh ground-truth metrics in `agent_metrics.json`.

## ✅ Actions Taken

### Fase 1 — Dependency bump
- `pyproject.toml`: `qgis-plugin-analyzer>=1.13.2` → `>=1.14.0` (both `dependencies` and `[dependency-groups].dev`).
- `uv.lock`: regenerated (`1.13.2` → `1.14.0`, resolved 74 packages).
- `.agent/memory/agent_metrics.json`: `tools.qgis-plugin-analyzer` → `1.14.0`.

### Fase 2 — Re-analysis
`uv run qgis-analyzer analyze .` (scope: all, 38 modules) produced materially different results from 1.13.2:

| Metric | 1.13.2 | 1.14.0 | Delta |
| :--- | :--- | :--- | :--- |
| Module Stability | 52.3 | 52.3 | = |
| Maintainability | 99.9 | 99.9 | = |
| Security | 100.0 | 100.0 | = |
| Type Hint Params | 94.3% | 90.4% | -3.9 |
| MISSING_I18N | 72 | 2 | -70 |
| Total issues | 75 | 5 | -70 |

The 1.14.0 i18n heuristic is far narrower than 1.13.2 — the 70 dropped `MISSING_I18N` findings were dict keys, CSS, logging and HTML markup previously flagged as false positives. The 2 remaining are `PerformanceTimer` labels (metric timing keys, developer-facing).

### Fase 3 — Metrics sync
- Updated `agent_metrics.json` summary: `type_hint_coverage_params` 94.3 → 90.4, `i18n_issues_qgis_analyzer` 72 → 2, `total_issues` 75 → 5, refreshed `issue_breakdown` and `ground_truth_sources.qgis_analyzer` (date, command `analyze .`, scores, issues).
- `sync_metrics.py --validate`: PASS (37 `.agent/` files metric-consistent).

## 📊 Operational Metrics
- Tests: 640/640 (Docker, `make docker-test`) — OK.
- `sync_metrics.py --validate`: PASS.
- Analyzer issues now: 2 `MISSING_I18N` (false positives), 2 `NON_PYTHONIC_LOOP`, 1 `SPATIAL_INDEX`.

## ⚠️ Pending
- Real technical debt unchanged by the version bump: 2 `NON_PYTHONIC_LOOP` (Goal 2.2), 1 `SPATIAL_INDEX` (Goal 2.3), `module_size_gate` FAIL (7 modules > 400 lines, Goal 2.4).
- Pre-existing uncommitted working-tree changes left as-is (not part of this session): `examples/sample_data/MURALLA.qgs`, `MURALLA.qgs~` (sample data regenerated by QGIS) and `resources/resources.py` (compiled resources).

## 📝 Lessons
- A tool upgrade can shift metrics more than a code change: 1.14.0 reduced i18n false positives by 70 and recomputed type-hint param coverage down 3.9 points. Always re-run `analyze .` (not just `summary`) after a version bump, since `summary` reads stale cached results.
